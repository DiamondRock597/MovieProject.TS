import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  activeIndicator: {
    justifyContent: 'center',
  },
  imageBlock: {
    alignItems: 'center',
  },
  imageCart: {
    borderRadius: 25,
  },
  headerHome: {
    paddingVertical: 20,
    fontSize: 20,
    color: 'black',
    opacity: 0.8,
  },
});
